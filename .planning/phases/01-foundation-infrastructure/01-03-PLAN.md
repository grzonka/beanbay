---
phase: 01-foundation-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - app/main.py
  - tests/conftest.py
  - tests/test_models.py
  - tests/test_optimizer.py
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
autonomous: true

must_haves:
  truths:
    - "Running `docker compose up` starts the app and it responds to HTTP requests"
    - "Health endpoint returns 200 with JSON status"
    - "SQLite database is created on first run inside the data volume"
    - "Campaign state persists across container restarts"
    - "Stopping and restarting the container preserves all data"
    - "BayBE integration test passes: create campaign, recommend, add measurement, recommend again"
    - "Model tests pass: create bean, create measurement, query relationships"
  artifacts:
    - path: "app/main.py"
      provides: "FastAPI application with lifespan, health endpoint, static/template mounts"
      contains: "FastAPI"
      min_lines: 25
    - path: "Dockerfile"
      provides: "Multi-stage Docker build with CPU-only PyTorch"
      contains: "FROM python"
    - path: "docker-compose.yml"
      provides: "Docker Compose config with volume mount and port mapping"
      contains: "brewflow-data"
    - path: "tests/conftest.py"
      provides: "Pytest fixtures for in-memory DB and test client"
      contains: "pytest.fixture"
    - path: "tests/test_models.py"
      provides: "Tests for Bean and Measurement SQLAlchemy models"
      contains: "def test_"
    - path: "tests/test_optimizer.py"
      provides: "Integration tests for OptimizerService"
      contains: "def test_"
  key_links:
    - from: "app/main.py"
      to: "app/services/optimizer.py"
      via: "creates OptimizerService in lifespan and stores on app.state"
      pattern: "OptimizerService"
    - from: "app/main.py"
      to: "app/database.py"
      via: "runs Alembic migrations or creates tables on startup"
      pattern: "Base.metadata.create_all|alembic"
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build context"
      pattern: "build:"
    - from: "tests/test_optimizer.py"
      to: "app/services/optimizer.py"
      via: "imports and tests OptimizerService"
      pattern: "from app.services.optimizer import"
---

<objective>
Build the FastAPI application skeleton, Docker deployment, and comprehensive tests for the entire Phase 1 foundation.

Purpose: Wire everything together into a deployable container with a health endpoint, and prove the foundation works via automated tests covering models, optimizer service, and the HTTP layer.
Output: A running Docker container serving HTTP on port 8000, plus a passing test suite that verifies all Phase 1 success criteria.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FastAPI app with health endpoint and Docker deployment</name>
  <files>
    app/main.py
    Dockerfile
    docker-compose.yml
    .dockerignore
  </files>
  <action>
    **Create app/main.py:**
    - Use asynccontextmanager for lifespan:
      - On startup: ensure data_dir and campaigns_dir exist (mkdir parents=True, exist_ok=True)
      - On startup: run Base.metadata.create_all(bind=engine) to create tables if they don't exist
      - On startup: create OptimizerService(settings.campaigns_dir) and store on app.state.optimizer
      - yield
      - On shutdown: nothing needed
    - Create FastAPI app with title="BrewFlow", lifespan=lifespan
    - Mount static files: app.mount("/static", StaticFiles(directory=Path(__file__).parent / "static"), name="static")
    - Create Jinja2Templates instance pointing to Path(__file__).parent / "templates"
    - Add health endpoint:
      ```
      @app.get("/health")
      async def health():
          return {"status": "ok", "service": "brewflow"}
      ```
    - Add root endpoint that returns a simple JSON for now (Phase 2 adds real pages):
      ```
      @app.get("/")
      async def root():
          return {"message": "BrewFlow is running", "docs": "/docs"}
      ```
    - Import from app.config, app.database, app.services.optimizer, app.models (to register models)

    **Create Dockerfile (multi-stage build):**
    ```dockerfile
    # Stage 1: Builder
    FROM python:3.11-slim AS builder
    WORKDIR /build
    
    # Install build dependencies
    RUN pip install --no-cache-dir uv
    
    # Copy dependency files first (for layer caching)
    COPY pyproject.toml ./
    
    # Install CPU-only PyTorch FIRST (critical — saves ~1GB vs default)
    RUN uv pip install --system torch --index-url https://download.pytorch.org/whl/cpu
    
    # Install project dependencies
    RUN uv pip install --system .
    
    # Stage 2: Runtime
    FROM python:3.11-slim AS runtime
    WORKDIR /app
    
    # Copy installed packages from builder
    COPY --from=builder /usr/local/lib/python3.11/site-packages /usr/local/lib/python3.11/site-packages
    COPY --from=builder /usr/local/bin /usr/local/bin
    
    # Copy application code
    COPY app/ ./app/
    COPY alembic.ini ./
    COPY migrations/ ./migrations/
    
    # Environment
    ENV CUDA_VISIBLE_DEVICES=""
    ENV BREWFLOW_DATA_DIR="/data"
    ENV PYTHONUNBUFFERED=1
    
    # Create data directory
    RUN mkdir -p /data/campaigns
    
    EXPOSE 8000
    
    CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
    ```

    **Create docker-compose.yml:**
    ```yaml
    services:
      brewflow:
        build: .
        ports:
          - "8000:8000"
        volumes:
          - brewflow-data:/data
        environment:
          - BREWFLOW_DATA_DIR=/data
        restart: unless-stopped
    
    volumes:
      brewflow-data:
    ```

    **Create .dockerignore:**
    ```
    .git
    .gitignore
    .planning
    .ruff_cache
    __pycache__
    *.pyc
    .venv
    venv
    data/
    tests/
    baybe-resources/
    __marimo__/
    *.egg-info
    ```

    IMPORTANT NOTES:
    - The StaticFiles mount should use check_dir=False or handle the case where static dirs are empty (only .gitkeep files). Alternatively, only mount if directory has real content. For Phase 1, use a try/except or conditional mount since we have no real static files yet.
    - Same for templates — Jinja2Templates init is fine even if directory is empty, just don't try to render templates yet.
    - The lifespan uses Base.metadata.create_all() for simplicity in Phase 1. This is safe alongside Alembic — create_all is a no-op if tables already exist.
  </action>
  <verify>
    Run locally: python -c "from app.main import app; print(app.title)"
    Run: uvicorn app.main:app --host 0.0.0.0 --port 8000 &amp; sleep 3 &amp;&amp; curl -s http://localhost:8000/health &amp;&amp; kill %1
    The health endpoint should return {"status":"ok","service":"brewflow"}
  </verify>
  <done>
    FastAPI app starts, serves /health returning 200 JSON. Dockerfile uses multi-stage build with CPU-only PyTorch. docker-compose.yml maps port 8000 and mounts persistent volume. .dockerignore excludes unnecessary files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive test suite</name>
  <files>
    tests/__init__.py
    tests/conftest.py
    tests/test_models.py
    tests/test_optimizer.py
  </files>
  <action>
    **Create tests/__init__.py (empty).**

    **Create tests/conftest.py:**
    - Fixture `db_engine` (session-scoped): create_engine("sqlite:///:memory:"), Base.metadata.create_all(engine), yield engine, engine.dispose()
    - Fixture `db_session` (function-scoped): create SessionLocal bound to db_engine, yield session, session.rollback(), session.close()
    - Fixture `tmp_campaigns_dir` (function-scoped): use tmp_path / "campaigns", mkdir, yield it
    - Fixture `optimizer_service` (function-scoped): OptimizerService(tmp_campaigns_dir), yield it
    - Import Base from app.database, Bean from app.models.bean, Measurement from app.models.measurement (to register models)

    **Create tests/test_models.py — test the SQLAlchemy models with in-memory SQLite:**
    
    test_create_bean:
    - Create Bean(name="Test Ethiopian", roaster="Onyx", origin="Yirgacheffe")
    - Add to session, commit, refresh
    - Assert id is a valid UUID string (len 36, contains hyphens)
    - Assert name, roaster, origin match
    - Assert created_at is not None

    test_create_measurement:
    - Create a bean first
    - Create Measurement with bean_id=bean.id, all 6 BayBE params (grind_setting=20.0, temperature=93.0, preinfusion_pct=75.0, dose_in=19.0, target_yield=40.0, saturation="yes"), taste=8.0
    - Commit and refresh
    - Assert all fields match
    - Assert id is auto-incremented integer

    test_bean_measurement_relationship:
    - Create bean, create 2 measurements for it
    - Assert len(bean.measurements) == 2
    - Assert each measurement.bean == bean
    - Assert each measurement.bean_id == bean.id

    test_measurement_recommendation_id_unique:
    - Create bean, create measurement with recommendation_id="rec-1"
    - Try to create another measurement with recommendation_id="rec-1"
    - Assert IntegrityError is raised (unique constraint)
    - Use pytest.raises(IntegrityError) and session.rollback()

    test_measurement_optional_fields:
    - Create measurement with only required fields (no extraction_time, notes, flavor columns)
    - Assert extraction_time is None, notes is None, all flavor columns are None
    - Assert is_failed defaults to False

    **Create tests/test_optimizer.py — test the BayBE integration:**
    
    Mark ALL tests in this file with @pytest.mark.slow (BayBE import takes 3-5s, recommend takes 2-10s).

    Also add a pytest.ini or pyproject.toml section to register the "slow" marker:
    ```toml
    [tool.pytest.ini_options]
    markers = ["slow: marks tests as slow (BayBE integration)"]
    asyncio_mode = "auto"
    ```

    test_create_campaign:
    - Create OptimizerService with tmp dir
    - Call get_or_create_campaign("test-bean")
    - Assert campaign is not None
    - Assert campaign JSON file exists on disk

    test_recommend_returns_all_params:
    - Call await service.recommend("test-bean")
    - Assert result is a dict
    - Assert all 6 param keys present: grind_setting, temperature, preinfusion_pct, dose_in, target_yield, saturation
    - Assert recommendation_id is present and non-empty
    - Assert param values are within bounds (15<=grind<=25, 86<=temp<=96, etc.)

    test_recommend_rounding:
    - Call await service.recommend("test-bean")
    - Assert grind_setting % 0.5 == 0
    - Assert temperature % 1.0 == 0
    - Assert preinfusion_pct % 5.0 == 0
    - Assert dose_in % 0.5 == 0
    - Assert target_yield % 1.0 == 0

    test_add_measurement_and_recommend_again:
    - Get first recommendation
    - Add measurement with the recommended params + taste=7.5
    - Get second recommendation
    - Assert second recommendation also has all 6 params (different values not guaranteed with 1 data point, but it should work)
    - Assert campaign file was updated on disk

    test_campaign_persistence_across_restart:
    - Create service, recommend, add measurement
    - Create NEW service instance pointing to same campaigns_dir (simulates restart)
    - Get campaign from new service
    - Assert campaign has measurements (measurement count > 0 or campaign.measurements is not empty)
    - Recommend from new service should work

    test_campaign_file_size_hybrid:
    - Create service, get recommendation, add measurement
    - Check campaign JSON file size
    - Assert file size < 500_000 bytes (should be <100KB for hybrid, vs 20MB for discrete)
    - Print actual size for visibility

    test_rebuild_campaign:
    - Create service, add 2-3 measurements
    - Call rebuild_campaign with a DataFrame of those measurements
    - Assert new campaign exists
    - Assert can recommend from rebuilt campaign

    Use pytest-asyncio for async tests. Each async test function should be `async def test_...` with the optimizer_service fixture.

    IMPORTANT: For the async tests, the optimizer_service fixture is sync (returns OptimizerService) and the tests call `await service.recommend(...)`. This works with pytest-asyncio's auto mode.

    Also add pytest-asyncio to dev dependencies in pyproject.toml if not already there:
    ```toml
    dev = [
        "pytest>=8.0",
        "pytest-asyncio>=0.23",
        "httpx>=0.27",
        "ruff>=0.4",
    ]
    ```
  </action>
  <verify>
    Run: python -m pytest tests/test_models.py -v
    Run: python -m pytest tests/test_optimizer.py -v -m slow
    Run: python -m pytest tests/ -v (all tests)
    All tests should pass.
  </verify>
  <done>
    - test_models.py: 5 tests pass covering Bean CRUD, Measurement CRUD, relationships, unique constraints, optional fields
    - test_optimizer.py: 7 tests pass covering campaign creation, recommendation with all params, rounding, measurement addition, persistence across restart, file size verification, campaign rebuild
    - All tests run green with `python -m pytest tests/ -v`
  </done>
</task>

<task type="auto">
  <name>Task 3: Build and verify Docker container</name>
  <files>
    (no new files — uses Dockerfile and docker-compose.yml from Task 1)
  </files>
  <action>
    Build and test the Docker container to verify the complete Phase 1 deliverable.

    1. Build the Docker image:
       ```
       docker compose build
       ```
       Verify it completes without errors.

    2. Start the container:
       ```
       docker compose up -d
       ```
    
    3. Wait for startup (BayBE import takes a few seconds):
       ```
       sleep 10
       ```

    4. Test health endpoint:
       ```
       curl -s http://localhost:8000/health
       ```
       Should return: {"status":"ok","service":"brewflow"}

    5. Test root endpoint:
       ```
       curl -s http://localhost:8000/
       ```
       Should return JSON indicating BrewFlow is running.

    6. Verify data persistence:
       ```
       docker compose down
       docker compose up -d
       sleep 10
       curl -s http://localhost:8000/health
       ```
       Should still return 200 — data volume survives restart.

    7. Check image size:
       ```
       docker images | grep brewflow
       ```
       Target: < 2GB (ideal < 1.5GB with CPU-only PyTorch).

    8. Clean up:
       ```
       docker compose down
       ```

    NOTE: If Docker build fails due to missing lock file (uv.lock), adjust the Dockerfile to use `pip install` directly instead of uv, or generate a requirements.txt from pyproject.toml first. The key constraint is CPU-only PyTorch installed BEFORE baybe.

    If the Docker build is not practical in the current environment (no Docker daemon), document the commands and verify the Dockerfile syntax is valid. The local tests (Task 2) prove the application logic works. Docker verification can be done on the target Unraid server.
  </action>
  <verify>
    If Docker is available:
    - `docker compose build` completes successfully
    - `curl http://localhost:8000/health` returns 200 with {"status":"ok","service":"brewflow"}
    - `docker compose down && docker compose up -d && sleep 10 && curl http://localhost:8000/health` returns 200 (persistence test)

    If Docker is NOT available:
    - Dockerfile syntax is valid (no obvious errors)
    - docker-compose.yml is valid YAML with correct service/volume config
    - All Python tests pass locally (proves app logic works independent of container)
  </verify>
  <done>
    Docker container builds, starts, and serves HTTP on port 8000. Health endpoint responds. Data volume is configured for persistence. Image uses CPU-only PyTorch. Container restarts preserve data.
  </done>
</task>

</tasks>

<verification>
Phase 1 Success Criteria verification:
1. `docker compose up` starts app, `curl localhost:8000/health` returns 200 ✓
2. SQLite database created on first run with beans and measurements tables ✓ (verified by test_models.py)
3. BayBE service layer: create campaign, recommend, add measurement ✓ (verified by test_optimizer.py)
4. Campaign JSON persists across restarts ✓ (verified by test_campaign_persistence_across_restart)
5. Container restart preserves data ✓ (verified by Docker restart test + volume mount)
</verification>

<success_criteria>
- FastAPI app starts and serves /health returning {"status":"ok","service":"brewflow"}
- All test_models.py tests pass (5 tests: create bean, create measurement, relationships, unique constraint, optional fields)
- All test_optimizer.py tests pass (7 tests: create campaign, recommend, rounding, add measurement, persistence, file size, rebuild)
- Docker image builds successfully with CPU-only PyTorch
- docker-compose.yml maps port 8000 and mounts brewflow-data volume at /data
- Container responds to HTTP requests after restart
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-03-SUMMARY.md`
</output>
