---
phase: 14-equipment-management
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/equipment.py
  - app/models/brew_setup.py
  - app/models/__init__.py
  - migrations/versions/XXXXXX_add_equipment_fields.py
autonomous: true

must_haves:
  truths:
    - "Grinder model has dial_type, step_size, min_value, max_value, is_retired fields"
    - "Brewer model has is_retired and many-to-many relationship with BrewMethod via association table"
    - "Paper model has description and is_retired fields"
    - "WaterRecipe model has individual mineral fields (gh, kh, ca, mg, na, cl, so4), notes, and is_retired"
    - "BrewSetup model has is_retired field"
    - "Alembic migration runs cleanly on existing database"
  artifacts:
    - path: "app/models/equipment.py"
      provides: "Extended Grinder, Brewer, Paper, WaterRecipe models with all required fields"
      contains: "dial_type"
    - path: "app/models/brew_setup.py"
      provides: "BrewSetup with is_retired field"
      contains: "is_retired"
    - path: "migrations/versions/"
      provides: "Alembic migration adding all new columns and association table"
  key_links:
    - from: "app/models/equipment.py"
      to: "app/database.py"
      via: "Base class inheritance"
      pattern: "class.*Base"
    - from: "Brewer"
      to: "BrewMethod"
      via: "brewer_methods association table"
      pattern: "brewer_methods"
---

<objective>
Extend the bare-bones Phase 13 equipment models with all fields required by CONTEXT.md and create the Alembic migration.

Purpose: Phase 13 models only have id/name/created_at. This plan adds dial configuration to grinders, method association to brewers, mineral fields to water recipes, description to papers, and is_retired to everything — the schema foundation all CRUD routes need.

Output: Updated SQLAlchemy models + Alembic migration that runs on existing databases.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-equipment-management/14-CONTEXT.md
@.planning/phases/13-data-model-evolution-bean-metadata/13-02-SUMMARY.md

@app/models/equipment.py
@app/models/brew_setup.py
@app/models/brew_method.py
@app/models/__init__.py
@app/database.py
@migrations/env.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SQLAlchemy models with all required fields</name>
  <files>app/models/equipment.py, app/models/brew_setup.py</files>
  <action>
Update `app/models/equipment.py` with these changes:

**Grinder model — add:**
- `dial_type = Column(String, nullable=False, default="stepless")` — values: "stepped" or "stepless"
- `step_size = Column(Float, nullable=True)` — only meaningful when dial_type="stepped"
- `min_value = Column(Float, nullable=True)` — minimum grind setting
- `max_value = Column(Float, nullable=True)` — maximum grind setting
- `is_retired = Column(Boolean, nullable=False, default=False)`

**Brewer model — add:**
- `is_retired = Column(Boolean, nullable=False, default=False)`
- `methods = relationship("BrewMethod", secondary="brewer_methods", backref="brewers")` — many-to-many

**Create association table** (in equipment.py, before the Brewer class):
```python
brewer_methods = Table(
    "brewer_methods",
    Base.metadata,
    Column("brewer_id", String, ForeignKey("brewers.id"), primary_key=True),
    Column("method_id", String, ForeignKey("brew_methods.id"), primary_key=True),
)
```

**Paper model — add:**
- `description = Column(String, nullable=True)`
- `is_retired = Column(Boolean, nullable=False, default=False)`

**WaterRecipe model — add:**
- `notes = Column(String, nullable=True)` — separate from recipe_details, for "how it was made"
- `gh = Column(Float, nullable=True)` — General Hardness
- `kh = Column(Float, nullable=True)` — Carbonate Hardness
- `ca = Column(Float, nullable=True)` — Calcium
- `mg = Column(Float, nullable=True)` — Magnesium
- `na = Column(Float, nullable=True)` — Sodium
- `cl = Column(Float, nullable=True)` — Chloride
- `so4 = Column(Float, nullable=True)` — Sulfate
- `is_retired = Column(Boolean, nullable=False, default=False)`

Update `app/models/brew_setup.py`:
- Add `is_retired = Column(Boolean, nullable=False, default=False)` to BrewSetup

Import `Boolean, Float, Table, ForeignKey` as needed in equipment.py. Use SQLAlchemy 1.x Column() style consistent with existing code. Import `relationship` in equipment.py for the Brewer.methods relationship.

Do NOT change the `__init__.py` exports — the existing exports already cover all model classes. Just add the `brewer_methods` table to the equipment.py module.
  </action>
  <verify>
`python -c "from app.models.equipment import Grinder, Brewer, Paper, WaterRecipe, brewer_methods; print('OK')"` succeeds.
`python -c "from app.models.brew_setup import BrewSetup; print(BrewSetup.is_retired)"` succeeds.
  </verify>
  <done>All 4 equipment models + BrewSetup have the extended fields. Grinder has dial_type/step_size/min_value/max_value. Brewer has many-to-many with BrewMethod. Paper has description. WaterRecipe has individual mineral fields + notes. All have is_retired.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration for schema changes</name>
  <files>migrations/versions/XXXXXX_add_equipment_fields.py</files>
  <action>
Generate Alembic migration with `alembic revision --autogenerate -m "add_equipment_fields_and_retire_lifecycle"`.

The migration must handle the same idempotency pattern from Phase 13-02: since `app/main.py` calls `Base.metadata.create_all()` at startup, columns may already exist. Use `inspector.get_table_names()` and `inspector.get_columns()` to check before adding.

**What the migration should create:**
1. New table: `brewer_methods` (association table with brewer_id + method_id PKs)
2. New columns on `grinders`: `dial_type` (String, default "stepless"), `step_size` (Float), `min_value` (Float), `max_value` (Float), `is_retired` (Boolean, default False)
3. New columns on `brewers`: `is_retired` (Boolean, default False)
4. New columns on `papers`: `description` (String), `is_retired` (Boolean, default False)
5. New columns on `water_recipes`: `notes` (String), `gh` (Float), `kh` (Float), `ca` (Float), `mg` (Float), `na` (Float), `cl` (Float), `so4` (Float), `is_retired` (Boolean, default False)
6. New column on `brew_setups`: `is_retired` (Boolean, default False)

**For existing rows**, set default values:
- `dial_type` = "stepless" for existing grinders
- `is_retired` = False for all existing rows

Use `render_as_batch=True` (already configured in env.py). Use named FK constraints for any foreign keys (pattern from 13-02).

After generating, verify: `alembic upgrade head` runs without errors. Then run `pytest` to confirm all 153+ tests still pass.
  </action>
  <verify>
`alembic upgrade head` succeeds.
`alembic current` shows the new migration as head.
`pytest` passes (all existing tests + no regressions).
  </verify>
  <done>Alembic migration exists and runs cleanly. Database schema has all equipment fields. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `python -c "from app.models.equipment import Grinder; g = Grinder.__table__; print([c.name for c in g.columns])"` shows dial_type, step_size, min_value, max_value, is_retired
- `python -c "from app.models.equipment import brewer_methods; print(brewer_methods)"` shows the association table
- `alembic current` shows the new migration at head
- `pytest` passes with 0 failures
</verification>

<success_criteria>
1. All equipment models have extended fields matching CONTEXT.md requirements
2. Brewer has many-to-many relationship with BrewMethod
3. All equipment models + BrewSetup have is_retired boolean
4. Alembic migration runs cleanly on existing database
5. All existing tests pass (153+)
</success_criteria>

<output>
After completion, create `.planning/phases/14-equipment-management/14-01-SUMMARY.md`
</output>
