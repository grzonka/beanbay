---
phase: 14-equipment-management
plan: "03"
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - app/routers/equipment.py
  - app/templates/equipment/index.html
  - app/templates/equipment/_paper_card.html
  - app/templates/equipment/_paper_form.html
  - app/templates/equipment/_water_card.html
  - app/templates/equipment/_water_form.html
  - tests/test_equipment.py
autonomous: true

must_haves:
  truths:
    - "User can create a paper/filter with name and optional description"
    - "User can edit an existing paper/filter"
    - "User can create a water recipe with name, notes, and optional mineral details"
    - "User can edit an existing water recipe"
    - "Water recipe form has expandable mineral details section"
    - "Equipment page shows all 4 types in collapsible sections with working CRUD"
    - "Tests verify CRUD operations for all 4 equipment types"
  artifacts:
    - path: "app/templates/equipment/_paper_form.html"
      provides: "Paper create/edit modal form"
      contains: "description"
    - path: "app/templates/equipment/_water_form.html"
      provides: "Water recipe create/edit modal with expandable mineral details"
      contains: "mineral"
    - path: "tests/test_equipment.py"
      provides: "CRUD tests for grinders, brewers, papers, water recipes"
      min_lines: 80
  key_links:
    - from: "app/routers/equipment.py"
      to: "app/models/equipment.py"
      via: "SQLAlchemy queries for Paper and WaterRecipe"
      pattern: "db\\.query\\(Paper\\)"
    - from: "tests/test_equipment.py"
      to: "app/routers/equipment.py"
      via: "TestClient HTTP requests"
      pattern: "client\\.(get|post).*equipment"
---

<objective>
Complete the equipment CRUD by adding paper/filter and water recipe management, then add tests for all 4 equipment types.

Purpose: Papers are simple (name + description), water recipes are the most detail-rich (mineral composition). Together with grinders and brewers from Plan 02, this completes the full equipment management CRUD.

Output: All 4 equipment types fully manageable, plus comprehensive test coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-equipment-management/14-CONTEXT.md
@.planning/phases/14-equipment-management/14-02-SUMMARY.md

@app/routers/equipment.py (has grinder/brewer routes from Plan 02)
@app/templates/equipment/index.html (has grinder/brewer sections from Plan 02)
@app/models/equipment.py (Paper, WaterRecipe models)
@tests/test_beans.py (test pattern reference)
@tests/conftest.py (fixtures)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paper/Filter CRUD routes and templates</name>
  <files>app/routers/equipment.py, app/templates/equipment/_paper_card.html, app/templates/equipment/_paper_form.html, app/templates/equipment/index.html</files>
  <action>
**Add paper routes to `app/routers/equipment.py`:**

`POST /equipment/papers` — Create paper. Form fields:
- `name: str = Form(...)` (required)
- `description: str = Form("")` (optional)
- Create Paper object, commit. htmx partial or redirect.

`GET /equipment/papers/{paper_id}/edit` — Return edit form partial.

`POST /equipment/papers/{paper_id}` — Update paper name and description.

**Create `app/templates/equipment/_paper_card.html`:**
- Simple card: paper name, description below (if present) in muted text
- Edit button (htmx modal pattern from Plan 02)
- Retired indicator if applicable

**Create `app/templates/equipment/_paper_form.html`:**
- Modal form: name input (required), description textarea (optional, placeholder "e.g. Cafec Abaca light roast")
- Create/edit mode based on `paper` variable presence

**Update equipment/index.html:**
- Replace the Papers/Filters placeholder section with actual loop + card rendering
- Add button for creating new paper
  </action>
  <verify>
Navigate to /equipment → Papers/Filters section shows.
Add paper with name + description → card appears.
Edit paper → form pre-filled, save works.
  </verify>
  <done>Papers/filters can be created and edited with name and optional description. Cards display in the equipment page.</done>
</task>

<task type="auto">
  <name>Task 2: Water Recipe CRUD routes and templates</name>
  <files>app/routers/equipment.py, app/templates/equipment/_water_card.html, app/templates/equipment/_water_form.html, app/templates/equipment/index.html</files>
  <action>
**Add water recipe routes to `app/routers/equipment.py`:**

`POST /equipment/water-recipes` — Create water recipe. Form fields:
- `name: str = Form(...)` (required)
- `notes: str = Form("")` (optional — "how it was made")
- Mineral fields (all optional, parse to float or None): `gh`, `kh`, `ca`, `mg`, `na`, `cl`, `so4`
- Create WaterRecipe, commit. htmx partial or redirect.

`GET /equipment/water-recipes/{recipe_id}/edit` — Return edit form partial.

`POST /equipment/water-recipes/{recipe_id}` — Update water recipe.

**Create `app/templates/equipment/_water_card.html`:**
- Card showing: water recipe name, notes (if present) in muted text
- If any mineral fields are set, show a compact summary line, e.g., "GH: 65 | KH: 40 | Ca: 30" — only show minerals that have values
- Edit button (htmx modal pattern)
- Retired indicator if applicable

**Create `app/templates/equipment/_water_form.html`:**
- Modal form for create/edit
- Name input (required)
- Notes textarea (optional, placeholder "e.g. Third Wave Water Classic Light profile, mixed in 1 gallon distilled")
- Expandable "Mineral Details" section (use the collapsible-toggle pattern or a `<details>` element):
  - 7 number inputs: GH (ppm), KH (ppm), Ca (ppm), Mg (ppm), Na (ppm), Cl (ppm), SO4 (ppm)
  - All optional, type="number" step="any" min="0"
  - Arranged in a compact grid (2 columns on mobile, 3-4 on wider)
- Pre-fill values for edit mode

**Update equipment/index.html:**
- Replace the Water Recipes placeholder section with actual loop + card rendering
- Add button for creating new water recipe
  </action>
  <verify>
Navigate to /equipment → Water Recipes section shows.
Add water recipe with name + notes → card appears.
Expand mineral details, add some values → saved correctly.
Edit recipe → all values pre-filled including minerals.
  </verify>
  <done>Water recipes can be created and edited with name, notes, and optional mineral composition. Expandable mineral details section works.</done>
</task>

<task type="auto">
  <name>Task 3: Equipment CRUD tests for all 4 types</name>
  <files>tests/test_equipment.py</files>
  <action>
**Create `tests/test_equipment.py`** following the pattern from `tests/test_beans.py`:

Use `client` and `db_session` fixtures from conftest.py.

**Fixtures:**
- `sample_grinder(db_session)` — creates a Grinder with name, dial_type="stepped", step_size=0.5, min_value=1, max_value=40
- `sample_brewer(db_session)` — creates a Brewer with name, plus an associated BrewMethod
- `sample_paper(db_session)` — creates a Paper with name and description
- `sample_water_recipe(db_session)` — creates a WaterRecipe with name, notes, gh=65, kh=40
- `espresso_method(db_session)` — creates or gets the Espresso BrewMethod

**Test cases (minimum):**

Equipment page:
- `test_equipment_page_loads` — GET /equipment returns 200 with "Equipment" in page
- `test_equipment_page_shows_counts` — with sample data, page shows count badges

Grinder tests:
- `test_create_grinder_stepped` — POST /equipment/grinders with stepped config → redirect, grinder in DB
- `test_create_grinder_stepless` — POST with stepless → redirect, grinder in DB with dial_type="stepless"
- `test_edit_grinder` — POST /equipment/grinders/{id} updates name and dial config

Brewer tests:
- `test_create_brewer_with_methods` — POST /equipment/brewers with method_ids → brewer created with associations
- `test_edit_brewer` — update name and method associations

Paper tests:
- `test_create_paper` — POST /equipment/papers → paper in DB
- `test_create_paper_with_description` — paper has description
- `test_edit_paper` — update name and description

Water recipe tests:
- `test_create_water_recipe_basic` — name + notes only
- `test_create_water_recipe_with_minerals` — all 7 mineral fields
- `test_edit_water_recipe` — update name, notes, minerals

Each test should verify:
1. HTTP response status (303 redirect for POST, 200 for GET)
2. Database state (query and assert the created/updated object)
3. No regressions (existing tests still pass)

Run `pytest tests/test_equipment.py -v` to verify all new tests pass, then `pytest` for full suite.
  </action>
  <verify>
`pytest tests/test_equipment.py -v` — all new tests pass.
`pytest` — full suite passes (153 existing + new equipment tests).
  </verify>
  <done>Comprehensive test coverage for all 4 equipment types: grinder (stepped/stepless), brewer (with methods), paper (with description), water recipe (with minerals). Full test suite passes.</done>
</task>

</tasks>

<verification>
- All 4 equipment types have working CRUD at /equipment
- Water recipe mineral details are expandable/collapsible
- Equipment page shows all sections with count badges
- `pytest tests/test_equipment.py -v` passes
- `pytest` full suite passes (153+ existing + new)
</verification>

<success_criteria>
1. Papers: create, edit with name + optional description
2. Water recipes: create, edit with name, notes, and optional mineral fields (7 minerals)
3. Equipment page complete — all 4 types with cards, forms, collapsible sections
4. Test coverage: 12+ new tests for equipment CRUD
5. All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/14-equipment-management/14-03-SUMMARY.md`
</output>
