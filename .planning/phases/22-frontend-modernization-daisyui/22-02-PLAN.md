---
phase: 22-frontend-modernization-daisyui
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/templates/beans/list.html
  - app/templates/beans/detail.html
  - app/templates/beans/_bean_card.html
  - app/templates/beans/_active_indicator.html
  - app/templates/brew/index.html
  - app/templates/brew/recommend.html
  - app/templates/brew/manual.html
  - app/templates/brew/best.html
  - app/templates/brew/_recipe_card.html
  - app/templates/brew/_feedback_panel.html
  - app/templates/brew/_recommendation_insights.html
autonomous: true

must_haves:
  truths:
    - "Bean list page shows bean cards with daisyUI card styling"
    - "Bean detail page shows bean info with proper card layout"
    - "Brew index shows setup/bean selectors with daisyUI select and card components"
    - "Get Recommendation, Repeat Best, and Manual Input buttons are large touch-friendly daisyUI buttons"
    - "Recommendation page shows recipe params in readable grid + feedback panel with sliders"
    - "Manual brew page shows param input sliders with number inputs"
    - "Best recipe page shows recipe params and feedback panel"
    - "All htmx attributes, Jinja2 logic, and JavaScript behavior preserved"
  artifacts:
    - path: "app/templates/beans/list.html"
      provides: "Bean list with daisyUI cards"
      contains: "card bg-base-200"
    - path: "app/templates/brew/index.html"
      provides: "Brew page with daisyUI selects and buttons"
      contains: "select select-bordered"
    - path: "app/templates/brew/recommend.html"
      provides: "Recommendation page with recipe display"
      contains: "recipe-params"
    - path: "app/templates/brew/_feedback_panel.html"
      provides: "Feedback panel with taste slider and flavor sliders"
      contains: "score-slider"
  key_links:
    - from: "app/templates/brew/index.html"
      to: "/brew/recommend"
      via: "form POST action"
      pattern: "action=\"/brew/recommend\""
    - from: "app/templates/brew/recommend.html"
      to: "app/templates/brew/_feedback_panel.html"
      via: "Jinja2 include"
      pattern: "include.*_feedback_panel"
    - from: "app/templates/brew/recommend.html"
      to: "app/templates/brew/_recipe_card.html"
      via: "Jinja2 include"
      pattern: "include.*_recipe_card"
---

<objective>
Restyle all beans/ (4 templates) and brew/ (7 templates) templates with daisyUI components and Tailwind utility classes.

Purpose: The beans and brew sections are the core user workflow (select bean → get recommendation → record feedback). They must look polished with the coffee theme while preserving all htmx interactions, form submissions, and JavaScript behavior.

Output: 11 templates restyled with daisyUI cards, buttons, forms, badges, and alerts. All inline styles replaced with Tailwind classes. Custom components (recipe-params, score-slider, flavor-slider) use the custom CSS from input.css.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-frontend-modernization-daisyui/22-RESEARCH.md
@.planning/phases/22-frontend-modernization-daisyui/22-01-SUMMARY.md

@app/templates/beans/list.html
@app/templates/beans/detail.html
@app/templates/beans/_bean_card.html
@app/templates/beans/_active_indicator.html
@app/templates/brew/index.html
@app/templates/brew/recommend.html
@app/templates/brew/manual.html
@app/templates/brew/best.html
@app/templates/brew/_recipe_card.html
@app/templates/brew/_feedback_panel.html
@app/templates/brew/_recommendation_insights.html
@app/static/css/input.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle beans/ templates (4 files)</name>
  <files>app/templates/beans/list.html, app/templates/beans/detail.html, app/templates/beans/_bean_card.html, app/templates/beans/_active_indicator.html</files>
  <action>
Restyle all 4 beans templates. Replace all custom CSS classes and inline styles with daisyUI components + Tailwind utilities.

**Component mapping for beans:**

| Old Pattern | daisyUI Replacement |
|---|---|
| `class="card"` | `class="card bg-base-200 border border-base-300"` |
| `class="card-title"` | Inside a `<div class="card-body">` wrapper, use `<h2 class="card-title">` |
| `class="card-header"` | `<div class="flex items-center justify-between">` inside card-body |
| `class="card-meta"` | `<span class="text-sm text-base-content/50">` |
| `class="badge badge-active"` | `<span class="badge badge-primary">` |
| `class="badge badge-count"` | `<span class="badge badge-ghost">` |
| `class="btn btn-primary"` | `class="btn btn-primary"` (same class names!) |
| `class="btn btn-secondary btn-sm"` | `class="btn btn-ghost btn-sm"` |
| `class="btn btn-danger btn-sm"` | `class="btn btn-error btn-sm"` |
| `class="page-header"` | `<div class="flex items-center justify-between mb-6 min-h-12">` |
| `class="page-title"` | `<h1 class="text-xl font-bold">` |
| `class="empty-state"` | `<div class="text-center py-8 text-base-content/50">` |
| `class="empty-state-title"` | `<div class="text-lg font-semibold text-base-content/70 mb-2">` |
| `class="form-group"` | `<div class="form-control mb-4">` (daisyUI form-control) |
| `class="form-label"` | `<label class="label"><span class="label-text">` |
| `class="form-input"` | `<input class="input input-bordered w-full">` |
| `class="form-hint"` | `<label class="label"><span class="label-text-alt">` |
| `class="text-muted"` | `class="text-base-content/40"` |
| `class="text-secondary"` | `class="text-base-content/70"` |
| `class="text-sm"` | `class="text-sm"` (Tailwind native) |
| `class="mb-md"` | `class="mb-4"` |
| `class="mt-sm"` | `class="mt-2"` |

**Spacing mapping:** `var(--spacing-xs)` → `1` (0.25rem), `var(--spacing-sm)` → `2` (0.5rem), `var(--spacing-md)` → `4` (1rem), `var(--spacing-lg)` → `6` (1.5rem), `var(--spacing-xl)` → `8` (2rem).

**For `_bean_card.html`:**
- Wrap the card content in `<div class="card-body p-4">` inside the `<div class="card bg-base-200 ...">`
- Replace all inline `style=` attributes with Tailwind utility classes
- Preserve all Jinja2 conditionals (active bean, roaster, bags, etc.)
- Preserve `hx-post`, `hx-target`, `hx-swap` attributes exactly
- Touch targets: buttons must have `min-h-12` (48px)

**For `_active_indicator.html`:**
- This is a small partial swapped by htmx — keep it small
- Replace custom classes with daisyUI equivalents

**For `list.html`:**
- Replace `page-header` div with Tailwind flex layout
- Replace bean list container styles
- "Add Bean" button: `btn btn-primary`
- Empty state: centered text with subdued colors

**For `detail.html`:**
- Card layout for bean details
- Form inputs: `input input-bordered` with `w-full`
- Textarea: `textarea textarea-bordered`
- Buttons: daisyUI btn classes
- All inline styles → Tailwind utilities

**CRITICAL — Preserve exactly:**
- All `hx-*` attributes unchanged
- All Jinja2 `{% if %}`, `{% for %}`, `{{ }}` expressions unchanged
- All `id=` attributes (used by htmx targeting)
- All `name=` attributes on form inputs
- All `method=` and `action=` on forms
- All `onclick` handlers if any exist
  </action>
  <verify>
    - All 4 files exist and contain daisyUI classes
    - No file contains old custom CSS classes: `card-link`, `nav-drawer-link`, `form-input` (as sole class), `badge-active`, `empty-state` (as CSS class)
    - All files still contain their original `hx-*` attributes
    - All files still contain their original Jinja2 blocks
    - `grep -r "style=" app/templates/beans/` returns minimal results (only truly necessary inline styles)
  </verify>
  <done>4 beans templates fully restyled with daisyUI card, badge, btn, form-control components. All htmx and Jinja2 logic preserved. No inline styles remain except where no Tailwind equivalent exists.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle brew/ templates (7 files)</name>
  <files>app/templates/brew/index.html, app/templates/brew/recommend.html, app/templates/brew/manual.html, app/templates/brew/best.html, app/templates/brew/_recipe_card.html, app/templates/brew/_feedback_panel.html, app/templates/brew/_recommendation_insights.html</files>
  <action>
Restyle all 7 brew templates. The brew section is the most complex UI — heavy use of inline styles in `index.html`, custom slider patterns in `_feedback_panel.html`, and recipe display grids in `_recipe_card.html`.

**Component mapping (brew-specific):**

| Old Pattern | daisyUI / Tailwind Replacement |
|---|---|
| `class="form-input"` on selects | `class="select select-bordered w-full min-h-12"` |
| `class="btn btn-primary btn-full brew-action-btn"` | `class="btn btn-primary w-full text-xl min-h-16 rounded-xl"` |
| `class="btn btn-secondary btn-full brew-action-btn"` | `class="btn btn-ghost w-full text-xl min-h-16 rounded-xl border border-base-300"` |
| `class="card"` (setup/bean panels) | `class="card bg-base-200 border border-base-300"` with `<div class="card-body p-4">` |
| `class="brew-actions"` | `<div class="flex flex-col gap-4">` |
| `class="recommendation-insights"` | `<div class="card bg-base-200 border border-base-300"><div class="card-body p-4">` |
| `class="insight-badge insight-badge-random"` | `<span class="badge badge-info">` (for random), `<span class="badge badge-warning">` (for bayesian_early), `<span class="badge badge-accent">` (for bayesian) |
| `class="score-slider"` | Keep `score-slider` class (custom CSS in input.css) + add `range range-primary` for daisyUI base styling |
| `class="flavor-slider"` | Keep `flavor-slider` class (custom CSS) |
| `class="flavor-slider-row"` | Keep `flavor-slider-row` (custom CSS grid) |
| `class="failed-toggle"` | `<label class="flex items-center gap-4 min-h-12 cursor-pointer">` with `<input type="checkbox" class="checkbox checkbox-error">` |
| `class="feedback-notes"` | `class="textarea textarea-bordered w-full min-h-16"` |
| `class="tag-chip"` | `<span class="badge badge-outline gap-1">` |
| `class="tag-chip-remove"` | `<button class="btn btn-ghost btn-xs px-0">` |

**For `index.html` (CRITICAL — most inline styles in the app):**
- Replace ALL `style="..."` attributes with Tailwind utility classes
- Setup panel: `<div class="card bg-base-200 border border-base-300"><div class="card-body p-4">`
- "SETUP" label: `<div class="text-sm font-semibold text-base-content/70 mb-1 uppercase tracking-wider">`
- Setup name: `<div class="text-base font-semibold mb-1">`
- Select: `<select class="select select-bordered w-full min-h-12">`
- Two-panel flex: `<div class="flex flex-col gap-4 mb-6">`
- Action buttons need `min-h-16` for large touch targets

**For `_recipe_card.html`:**
- Keep `recipe-params` class (custom CSS grid)
- Keep `recipe-param` class (custom CSS)
- Keep `recipe-label`, `recipe-value`, `recipe-unit` classes (custom CSS)
- Keep `ratio` class (custom CSS)
- These are all in input.css's `@layer components`

**For `_feedback_panel.html`:**
- Keep `score-slider` class (custom CSS for slider thumb)
- Keep `flavor-slider` class with `.touched` JS pattern
- Keep `flavor-slider-row` class (custom CSS grid)
- Keep `#taste-group` with `.touched` opacity pattern
- Keep `taste-required-msg` with `.visible` pattern
- Replace `form-group` → `form-control mb-4`
- Replace `form-label` → `label` with `label-text`
- Replace `form-input` on text inputs → `input input-bordered w-full`
- Preserve ALL JavaScript: `onchange`, `oninput` handlers for slider value display and `.touched` class toggling
- Preserve all `name=` attributes on inputs

**For `recommend.html`:**
- Card wrappers for recipe + feedback sections
- `{% include %}` references to `_recipe_card.html`, `_feedback_panel.html`, `_recommendation_insights.html` — preserved
- Form `action` and `method` preserved
- Buttons: `btn btn-primary w-full min-h-12` for submit

**For `manual.html`:**
- Keep `param-input-row`, `param-slider`, `param-number` classes (custom CSS)
- Replace `form-group` → `form-control`
- Replace `form-label` → `label` + `label-text`
- Preserve all slider `min`, `max`, `step`, `value` attributes
- Preserve all JavaScript for slider ↔ number input sync

**For `best.html`:**
- Similar to recommend.html but for "best recipe" display
- Card layout with recipe params

**For `_recommendation_insights.html`:**
- Replace with daisyUI card + badge components
- `insight-badge-random` → use Tailwind bg + text colors: `badge bg-sky-900/50 text-sky-300`
- `insight-badge-bayesian` → `badge bg-amber-900/50 text-amber-300`
- `insight-badge-bayesian_early` → `badge bg-emerald-900/50 text-emerald-300`
- Keep `insight-prediction`, `insight-explanation` as Tailwind utility classes

**CRITICAL — Preserve exactly:**
- `hx-post="/brew/record"`, `hx-target`, `hx-swap` on feedback forms
- All JavaScript functions: slider sync, `.touched` toggling, form validation
- All `id=` attributes used by JS/htmx
- `method="post"` and `action=` on forms
- `{% include %}` directives
- All Jinja2 conditionals and loops
  </action>
  <verify>
    - All 7 files contain daisyUI/Tailwind classes
    - `brew/index.html` has zero or near-zero `style=` attributes (was heavily inline-styled)
    - `_feedback_panel.html` still has `score-slider`, `flavor-slider`, `.touched` JS pattern
    - `_recipe_card.html` still has `recipe-params`, `recipe-param`, `recipe-value` custom classes
    - All `hx-*` attributes preserved in all 7 files
    - All form `action=` and `method=` preserved
    - All `{% include %}` directives preserved
    - Grep for `var(--` returns zero results (no old CSS variables in templates)
  </verify>
  <done>7 brew templates fully restyled. brew/index.html inline styles replaced with Tailwind. Recipe display, feedback sliders, recommendation insights all use daisyUI + custom component classes. All htmx, Jinja2, and JavaScript behavior preserved.</done>
</task>

</tasks>

<verification>
- Navigate to /beans — card list renders with daisyUI styling, touch targets 48px+
- Add/edit bean — form inputs use daisyUI `input input-bordered`
- Navigate to /brew — setup and bean selection panels styled as daisyUI cards
- Select a bean, click "Get Recommendation" — recommendation page shows recipe params grid
- Taste slider starts faded, becomes opaque on touch
- Flavor sliders start faded, become opaque on touch
- Submit feedback — htmx POST works, page updates
- Navigate to /brew/manual — parameter sliders sync with number inputs
- Navigate to /brew/best — best recipe displays correctly
- All buttons have 48px+ minimum height
</verification>

<success_criteria>
1. All 11 templates (4 beans + 7 brew) use daisyUI/Tailwind classes exclusively
2. Zero custom CSS class references except those defined in input.css custom layer
3. Zero inline `style=` attributes in brew/index.html (was heavily inline-styled)
4. All htmx interactions work: form submissions, partial swaps, indicators
5. Taste slider inactive/active opacity pattern works
6. Flavor slider .touched pattern works
7. All buttons ≥48px touch target (min-h-12)
</success_criteria>

<output>
After completion, create `.planning/phases/22-frontend-modernization-daisyui/22-02-SUMMARY.md`
</output>
