---
phase: 20-espresso-parameter-evolution
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - app/services/optimizer.py
  - app/services/transfer_learning.py
  - app/routers/brew.py
  - app/models/campaign_state.py
  - tests/test_optimizer.py
  - tests/test_brew.py
autonomous: true

must_haves:
  truths:
    - "Campaigns are created with brewer-appropriate parameters (not always Tier 1)"
    - "User is prompted to rebuild campaign when brewer capabilities make param set outdated"
    - "Declining the rebuild prompt remembers the decision and stops nagging after one reminder"
    - "Campaign rebuild migrates existing measurements with recency bias"
    - "Transfer learning also builds campaigns with brewer-appropriate parameters"
  artifacts:
    - path: "app/services/optimizer.py"
      provides: "_create_fresh_campaign accepts brewer arg, param_set_fingerprint for structural change detection"
      contains: "brewer"
    - path: "app/services/transfer_learning.py"
      provides: "build_transfer_campaign accepts brewer arg"
      contains: "brewer"
    - path: "app/routers/brew.py"
      provides: "Passes brewer through to campaign creation, checks for outdated campaigns before brew"
      contains: "campaign_outdated"
    - path: "app/models/campaign_state.py"
      provides: "param_set_fingerprint and rebuild_declined columns"
      contains: "param_set_fingerprint"
  key_links:
    - from: "app/routers/brew.py"
      to: "app/services/optimizer.py"
      via: "passes brewer from active_setup to get_or_create_campaign"
      pattern: "brewer=.*\\.brewer"
    - from: "app/services/optimizer.py"
      to: "app/services/parameter_registry.py"
      via: "build_parameters_for_setup(method, brewer=brewer)"
      pattern: "brewer=brewer"
    - from: "app/routers/brew.py"
      to: "app/models/campaign_state.py"
      via: "checks param_set_fingerprint mismatch for outdated detection"
      pattern: "param_set_fingerprint"
---

<objective>
Wire brewer context through the campaign creation pipeline so campaigns get brewer-appropriate parameters (not always Tier 1). Add campaign outdated detection and rebuild prompt UX.

Purpose: This is the core Phase 20 behavioral change — campaigns actually use capability-gated parameters. Without this, the registry entries from Plan 01 are never activated.
Output: Brewer-aware campaigns, outdated detection, rebuild prompt flow with "remind once then quiet" behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-espresso-parameter-evolution/20-CONTEXT.md
@.planning/phases/20-espresso-parameter-evolution/20-01-SUMMARY.md

@app/services/optimizer.py
@app/services/transfer_learning.py
@app/services/parameter_registry.py
@app/routers/brew.py
@app/models/campaign_state.py
@app/models/brew_setup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire brewer into campaign creation + structural fingerprinting</name>
  <files>
    app/services/optimizer.py
    app/services/transfer_learning.py
    app/models/campaign_state.py
    migrations/versions/NEW_add_param_set_fingerprint.py
  </files>
  <action>
    **1a. Add `brewer` parameter to `_create_fresh_campaign`:**
    - Change the `@staticmethod` `_create_fresh_campaign(overrides, method)` signature to accept `brewer=None` as a third parameter.
    - Pass `brewer` to `build_parameters_for_setup(method, brewer=brewer, overrides=overrides)` instead of `brewer=None`.
    - This means campaigns will now get capability-gated parameters when a brewer is provided.

    **1b. Thread `brewer` through `get_or_create_campaign`:**
    - Add `brewer=None` parameter to `get_or_create_campaign()` signature.
    - Pass `brewer` to `self._create_fresh_campaign(overrides, method, brewer)` in all call sites within the method (lines ~229 and ~240).
    - Pass `brewer` to `build_transfer_campaign()` call (line ~218).

    **1c. Thread `brewer` through `recommend` and `add_measurement`:**
    - Add `brewer=None` to `recommend()` and `add_measurement()` signatures.
    - Pass through to `get_or_create_campaign()`.
    - Update `rebuild_campaign()` to also accept and pass `brewer`.
    - Update `get_recommendation_insights()` to accept `brewer` (for `get_param_columns` calls).

    **1d. Thread `brewer` through `build_transfer_campaign`:**
    - In `app/services/transfer_learning.py`, add `brewer=None` parameter to `build_transfer_campaign()`.
    - Pass `brewer` to `build_parameters_for_setup(method, brewer=brewer, overrides=overrides)` on line ~147.
    - Pass `brewer` to `get_param_columns(method, brewer)` on line ~55 and ~163.

    **1e. Add `param_set_fingerprint` to CampaignState:**
    - In `app/models/campaign_state.py`, add `param_set_fingerprint = Column(String, nullable=True)` — stores a hash of the parameter names in the campaign's search space.
    - Add `rebuild_declined = Column(Boolean, nullable=True, default=False)` — tracks whether user declined the rebuild prompt.
    - Create Alembic migration to add both columns to `campaign_states` table.

    **1f. Implement structural fingerprint computation:**
    - In `optimizer.py`, add a function `_param_set_fingerprint(method, brewer)` that returns a stable hash of the sorted parameter names from `get_param_columns(method, brewer)`.
    - When creating/saving a campaign, store this fingerprint in `CampaignState.param_set_fingerprint`.
    - This is SEPARATE from the existing `_bounds_fingerprint` (which tracks numeric range changes). `_param_set_fingerprint` tracks structural changes (params added/removed due to brewer capability changes).

    **1g. Add `is_campaign_outdated` check method:**
    - Add `OptimizerService.is_campaign_outdated(campaign_key, method, brewer)` that:
      1. Loads the stored `param_set_fingerprint` from CampaignState
      2. Computes current fingerprint from `get_param_columns(method, brewer)`
      3. Returns `True` if they differ (structural change), `False` if same or no stored fingerprint
    - Add `OptimizerService.was_rebuild_declined(campaign_key)` that returns the `rebuild_declined` flag from CampaignState.
    - Add `OptimizerService.decline_rebuild(campaign_key)` that sets `rebuild_declined = True` on the CampaignState row.
    - Add `OptimizerService.accept_rebuild(campaign_key, method, brewer, overrides)` that rebuilds the campaign with brewer-aware params, migrates measurements, clears `rebuild_declined`, and updates the `param_set_fingerprint`.
  </action>
  <verify>
    - `python -c "from app.services.optimizer import OptimizerService; print('OK')"` — imports cleanly
    - `python -c "from app.services.transfer_learning import build_transfer_campaign; import inspect; print('brewer' in inspect.signature(build_transfer_campaign).parameters)"` — prints True
    - `alembic upgrade head` — migration runs cleanly
    - `python -m pytest tests/test_optimizer.py -v -x` — passes
  </verify>
  <done>
    Brewer is threaded through all campaign creation paths. _create_fresh_campaign, get_or_create_campaign, recommend, add_measurement, rebuild_campaign, and build_transfer_campaign all accept brewer. Structural param_set_fingerprint is computed and stored. is_campaign_outdated, was_rebuild_declined, decline_rebuild, accept_rebuild methods work correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Brew router — pass brewer, campaign outdated prompt, rebuild flow</name>
  <files>
    app/routers/brew.py
    app/templates/brew/campaign_outdated.html
    app/routers/history.py
    tests/test_brew.py
  </files>
  <action>
    **2a. Pass brewer to all optimizer calls in brew.py:**
    - In `trigger_recommend()`: get brewer from `active_setup.brewer if active_setup else None`, pass as `brewer=brewer` to `optimizer.recommend()`.
    - In `record_measurement()`: same pattern, pass brewer to `optimizer.add_measurement()`.
    - In `show_best()` / `brew_again()`: pass brewer to `optimizer.get_or_create_campaign()` if called.
    - In `extend_ranges()`: pass brewer to optimizer calls.
    - This wires the brewer context that was previously always `None`.

    **2b. Add campaign outdated check before recommendation:**
    - In `trigger_recommend()`, BEFORE calling `optimizer.recommend()`:
      1. Call `optimizer.is_campaign_outdated(campaign_key, method, brewer)`
      2. If outdated AND NOT `optimizer.was_rebuild_declined(campaign_key)`:
         - Redirect to a "campaign outdated" prompt page instead of generating recommendation
      3. If not outdated OR already declined: proceed normally with recommendation
    - The check is lightweight (string comparison of fingerprints).

    **2c. Create campaign outdated prompt template:**
    - Create `app/templates/brew/campaign_outdated.html`:
      - Message: "Your brewer now supports additional parameters. Rebuild campaign to include them?"
      - Show which new params would be added (diff between old and new param sets)
      - Two buttons: "Rebuild Campaign" (POST /brew/rebuild-campaign) and "Skip for Now" (POST /brew/decline-rebuild)
      - Style with existing daisyUI card/button patterns (see recommend.html for reference)

    **2d. Add rebuild and decline routes:**
    - `POST /brew/rebuild-campaign`:
      1. Call `optimizer.accept_rebuild(campaign_key, method, brewer, overrides)` — this rebuilds the campaign with new params, migrates old measurements
      2. Redirect to `/brew/recommend` (trigger fresh recommendation with new params)
    - `POST /brew/decline-rebuild`:
      1. Call `optimizer.decline_rebuild(campaign_key)`
      2. Redirect to `/brew` with a flash/message or just back to brew index
      3. "Remind once then quiet" logic: first decline sets `rebuild_declined = True`. On next session (or next brew attempt), show reminder once more, then set a permanent flag. Implementation: `rebuild_declined` can be an integer: 0 = not declined, 1 = declined once (will remind), 2 = permanently declined.

    **2e. Fix history.py TODO(Phase 20):**
    - At line 335 in `history.py`, the `delete_batch` handler hardcodes `get_param_columns("espresso")`. Fix:
      1. For each affected bean, look up the measurement's brew_setup to determine the method.
      2. Get the brewer from the setup for capability-aware param columns.
      3. Use `get_param_columns(method, brewer)` instead of `get_param_columns("espresso")`.
      4. Also pass `method=method` and `brewer=brewer` to `optimizer.rebuild_campaign()`.
    - If measurements have no brew_setup (legacy), fall back to `method="espresso"` and `brewer=None`.

    **2f. Add/update tests:**
    - Test that `trigger_recommend` with an active setup passes brewer context (mock optimizer, verify brewer arg).
    - Test campaign outdated detection flow: mock `is_campaign_outdated` returning True → user sees prompt.
    - Test rebuild route: rebuilds campaign, redirects to recommend.
    - Test decline route: sets flag, redirects to brew.
    - Test history delete_batch uses method-aware param columns.
  </action>
  <verify>
    - `python -m pytest tests/test_brew.py -v -x` — passes
    - `python -m pytest tests/ -x` — all tests pass
    - Manual check: `rg "brewer=None" app/services/optimizer.py` — only appears in function signatures as default, NOT in call sites from brew.py
  </verify>
  <done>
    Brew router passes brewer to all optimizer calls. Campaign outdated detection works — prompts user when param set changes. Rebuild and decline routes implemented with "remind once then quiet" behavior. history.py delete_batch is method-aware. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest -x` — all tests pass
- Brewer is threaded through: `_create_fresh_campaign`, `get_or_create_campaign`, `recommend`, `add_measurement`, `rebuild_campaign`, `build_transfer_campaign`
- Campaign created with brewer having flow control includes saturation, flow_rate in search space
- Campaign created with brewer=None still produces Tier 1 (4 params) — backward compatible
- `is_campaign_outdated` correctly detects when brewer capabilities change the param set
- `decline_rebuild` / `accept_rebuild` routes work
- history.py delete_batch no longer hardcodes espresso
</verification>

<success_criteria>
- Campaigns created with brewer context use capability-appropriate parameters
- No more `brewer=None` hardcoded in call sites (only as default parameters)
- Campaign outdated detection works via param_set_fingerprint comparison
- User sees rebuild prompt when brewer capabilities change
- "Remind once then quiet" — two declines permanently silences the prompt
- Transfer learning also uses brewer context
- history.py is method-aware for campaign rebuilds after batch delete
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-espresso-parameter-evolution/20-02-SUMMARY.md`
</output>
