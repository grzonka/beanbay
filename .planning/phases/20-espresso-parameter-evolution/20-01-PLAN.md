---
phase: 20-espresso-parameter-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/equipment.py
  - app/models/measurement.py
  - app/services/parameter_registry.py
  - migrations/versions/f7a2c91b3d04_add_espresso_parameter_evolution_columns.py
  - migrations/versions/NEW_rename_preinfusion_saturation_rework.py
  - tests/test_parameter_registry.py
autonomous: true

must_haves:
  truths:
    - "preinfusion_pct column is renamed to preinfusion_pressure_pct in the database"
    - "saturation parameter is a boolean toggle (yes/no) gated by flow_control_type != 'none', NOT legacy"
    - "Brewer model has saturation_flow_rate column"
    - "preinfusion_pressure, bloom_pause, temp_profile have registry entries with correct capability gates"
    - "Existing data is preserved through the column rename migration"
  artifacts:
    - path: "migrations/versions/NEW_rename_preinfusion_saturation_rework.py"
      provides: "Alembic migration: rename preinfusion_pct→preinfusion_pressure_pct, add saturation_flow_rate to brewers"
    - path: "app/services/parameter_registry.py"
      provides: "Updated espresso registry with preinfusion_pressure, bloom_pause, temp_profile entries + saturation rework"
      contains: "preinfusion_pressure"
    - path: "app/models/equipment.py"
      provides: "Brewer.saturation_flow_rate column"
      contains: "saturation_flow_rate"
    - path: "app/models/measurement.py"
      provides: "preinfusion_pressure_pct column (renamed from preinfusion_pct)"
      contains: "preinfusion_pressure_pct"
  key_links:
    - from: "app/services/parameter_registry.py"
      to: "app/models/equipment.py"
      via: "capability gate conditions reference brewer attributes"
      pattern: "brewer\\.has_bloom|brewer\\.flow_control_type"
    - from: "migrations/versions/NEW_*.py"
      to: "app/models/measurement.py"
      via: "column rename must match ORM column name"
      pattern: "preinfusion_pressure_pct"
---

<objective>
Update the database schema and parameter registry to implement the CONTEXT.md overrides:
1. Rename `preinfusion_pct` → `preinfusion_pressure_pct` (column rename, NOT a data conversion — it was always pump pressure percentage)
2. Rework `saturation` from legacy to active boolean toggle, gated by `flow_control_type != 'none'`; add `saturation_flow_rate` to Brewer model
3. Add missing registry entries: `preinfusion_pressure`, `bloom_pause`, `temp_profile` with correct capability gates

Purpose: Foundation for all other Phase 20 work. The registry and schema must be correct before wiring brewer context into campaigns.
Output: Updated models, registry, new Alembic migration, passing tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-espresso-parameter-evolution/20-CONTEXT.md

@app/models/measurement.py
@app/models/equipment.py
@app/services/parameter_registry.py
@migrations/versions/f7a2c91b3d04_add_espresso_parameter_evolution_columns.py
@tests/test_parameter_registry.py
@app/routers/brew.py
@app/templates/brew/_recipe_card.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema updates — column rename, saturation_flow_rate, Alembic migration</name>
  <files>
    app/models/measurement.py
    app/models/equipment.py
    migrations/versions/NEW_rename_preinfusion_saturation_rework.py
    migrations/versions/f7a2c91b3d04_add_espresso_parameter_evolution_columns.py
  </files>
  <action>
    **1a. Rename `preinfusion_pct` → `preinfusion_pressure_pct` in Measurement model:**
    - In `app/models/measurement.py`, rename the column definition from `preinfusion_pct = Column(Float, nullable=True)` to `preinfusion_pressure_pct = Column(Float, nullable=True)`. Keep the comment updated.
    - This is a RENAME, not a new column. The data stays the same — preinfusion_pct was always pump pressure percentage (55-100%), NOT a time proxy.

    **1b. Add `saturation_flow_rate` to Brewer model:**
    - In `app/models/equipment.py`, add `saturation_flow_rate = Column(Float, nullable=True)` to the Brewer class, below the `flow_control_type` field. Add a comment: `# ml/s — fixed brewer-level setting for saturation flow rate (e.g., 1.5 for Sage DB slayer mod)`
    - This is a brewer-level setting, NOT a per-shot BayBE optimization parameter.

    **1c. Create new Alembic migration:**
    - Generate a new migration file (use `alembic revision --autogenerate -m "rename_preinfusion_pct_add_saturation_flow_rate"` or create manually).
    - The migration must:
      1. Rename column `preinfusion_pct` → `preinfusion_pressure_pct` on `measurements` table using `batch_op.alter_column('preinfusion_pct', new_column_name='preinfusion_pressure_pct')`.
      2. Add `saturation_flow_rate` (Float, nullable) to `brewers` table.
    - Use SQLite-safe batch mode for the rename. Use idempotent checks (inspect column names before altering) like the existing migration does.
    - Set `down_revision` to `f7a2c91b3d04` (the existing Phase 20 migration).

    **1d. Update the EXISTING Phase 20 migration:**
    - In `f7a2c91b3d04`, the data migration converts `preinfusion_pct / 100 * 15.0` to `preinfusion_time`. This is WRONG per CONTEXT.md — preinfusion_pct is pump pressure %, not a time proxy.
    - Remove the data migration SQL (lines 69-78) entirely. The column will be renamed, not converted.
    - Update the docstring to reflect this change: remove mention of "Data migration: preinfusion_pct → preinfusion_time".
    - Keep step 1 (add new columns) and step 3 (relax NOT NULL) as-is.

    **1e. Global rename in codebase:**
    - Search all Python and HTML files for `preinfusion_pct` and rename to `preinfusion_pressure_pct`. Key locations:
      - `app/routers/brew.py`: `_MEASUREMENT_FLOAT_COLUMNS` set, `_extract_params_from_form` (if referenced), manual brew prefill logic
      - `app/templates/brew/_recipe_card.html`: Jinja references to `rec.preinfusion_pct` → `rec.preinfusion_pressure_pct`
      - `app/templates/brew/best.html`: hidden input `name="preinfusion_pct"` → `name="preinfusion_pressure_pct"`
      - `app/services/parameter_registry.py`: legacy param entry name
      - Any test files that reference `preinfusion_pct`
    - Use `rg preinfusion_pct` to find ALL references. Do NOT miss any.
  </action>
  <verify>
    - `rg "preinfusion_pct" --type py --type html` returns ZERO hits (only `preinfusion_pressure_pct`)
    - `alembic upgrade head` runs without error
    - `python -c "from app.models.measurement import Measurement; print(hasattr(Measurement, 'preinfusion_pressure_pct'))"` prints True
    - `python -c "from app.models.equipment import Brewer; print(hasattr(Brewer, 'saturation_flow_rate'))"` prints True
  </verify>
  <done>
    preinfusion_pct column renamed to preinfusion_pressure_pct everywhere (model, migration, templates, router, registry, tests). Brewer has saturation_flow_rate column. Existing migration's incorrect data conversion removed. New migration runs cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Registry updates — saturation rework + missing params + tests</name>
  <files>
    app/services/parameter_registry.py
    tests/test_parameter_registry.py
  </files>
  <action>
    **2a. Rework saturation in the registry:**
    - Change `saturation` from `legacy: True` to `legacy: False`.
    - Change its `requires` from `None` to `"brewer.flow_control_type in (manual_paddle, manual_valve, programmable)"` — any brewer with flow control can use saturation.
    - Keep type as `categorical`, values as `["yes", "no"]`, encoding as `"OHE"`.
    - Add a comment: `# Saturation toggle: perform reduced-flow pre-wetting. Gated by flow control capability.`

    **2b. Add missing registry entries for espresso:**
    - Add `preinfusion_pressure` entry (Tier 2, alongside preinfusion_time):
      ```python
      {
          "name": "preinfusion_pressure",
          "type": "continuous",
          "bounds": (1.0, 6.0),
          "rounding": 0.5,
          "requires": "brewer.preinfusion_type in (adjustable_pressure, programmable)",
          "legacy": False,
      },
      ```
    - Add `bloom_pause` entry (gated on has_bloom):
      ```python
      {
          "name": "bloom_pause",
          "type": "continuous",
          "bounds": (0.0, 10.0),
          "rounding": 1.0,
          "requires": "brewer.has_bloom == True",
          "legacy": False,
      },
      ```
    - Add `temp_profile` entry (gated on temp profiling capability):
      ```python
      {
          "name": "temp_profile",
          "type": "categorical",
          "values": ["flat", "ramp_up", "ramp_down"],
          "encoding": "OHE",
          "rounding": None,
          "requires": "brewer.temp_control_type in (profiling)",
          "legacy": False,
      },
      ```
    - Place these in the correct tier positions within the espresso list. Order should be:
      Tier 1 (grind, temp, dose, yield) → Tier 2 (preinfusion_time, preinfusion_pressure) → Tier 3 (brew_pressure) → Tier 4 (pressure_profile) → Tier 5 (flow_rate, brew_mode, saturation) → bloom_pause, temp_profile → Legacy (preinfusion_pressure_pct)

    **2c. Update `requires_check` if needed:**
    - Verify the `requires_check()` function can handle the condition string `"brewer.has_bloom == True"`. If the current parser only supports `in (...)` syntax, extend it to handle `== True` / `== False` comparisons.

    **2d. Update tests:**
    - Add tests for:
      - `get_param_columns("espresso", brewer=None)` still returns 4 Tier 1 params (saturation is now gated, not legacy, so it's excluded when brewer=None)
      - `get_param_columns("espresso", brewer_with_flow_control)` includes `saturation`
      - `get_param_columns("espresso", brewer_with_adjustable_pressure_preinfusion)` includes `preinfusion_pressure`
      - `get_param_columns("espresso", brewer_with_bloom)` includes `bloom_pause`
      - `get_param_columns("espresso", brewer_with_temp_profiling)` includes `temp_profile`
      - Legacy `preinfusion_pressure_pct` is excluded from `get_param_columns` results
    - Create mock brewer objects (SimpleNamespace or similar) with capability attributes for testing.
  </action>
  <verify>
    - `python -m pytest tests/test_parameter_registry.py -v` passes
    - `python -m pytest -x` — all 337+ tests pass
    - `python -c "from app.services.parameter_registry import get_param_columns; print(get_param_columns('espresso'))"` returns 4 Tier 1 params
  </verify>
  <done>
    Registry has all espresso params with correct capability gates. Saturation is active (not legacy) with flow_control gate. preinfusion_pressure, bloom_pause, temp_profile have registry entries. All existing + new tests pass.
  </done>
</task>

</tasks>

<verification>
- `rg "preinfusion_pct[^_]" --type py --type html` returns zero matches (only preinfusion_pressure_pct references remain)
- `alembic upgrade head` completes without error
- `python -m pytest -x` — all tests pass
- Registry: `get_param_columns("espresso")` returns exactly `["grind_setting", "temperature", "dose_in", "target_yield"]` (Tier 1 only, brewer=None)
- Registry: brewer with all capabilities returns all espresso params including saturation, preinfusion_pressure, bloom_pause, temp_profile
</verification>

<success_criteria>
- preinfusion_pct renamed to preinfusion_pressure_pct across entire codebase (zero leftover references)
- saturation is active with flow_control gate, NOT legacy
- Brewer.saturation_flow_rate column exists
- preinfusion_pressure, bloom_pause, temp_profile in registry with correct gates
- Existing migration's incorrect data conversion removed
- New Alembic migration runs cleanly on existing databases
- All tests pass (existing + new registry tests)
</success_criteria>

<output>
After completion, create `.planning/phases/20-espresso-parameter-evolution/20-01-SUMMARY.md`
</output>
