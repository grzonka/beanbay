---
phase: 08-documentation-release
plan: 01
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "A new user can understand what BeanBay is from the README"
    - "A new user can run the app via Docker using instructions in README"
    - "The README clearly indicates the project is work-in-progress"
    - "The project has a clear open-source license (Apache 2.0, already present)"
  artifacts:
    - path: "README.md"
      provides: "Concise project documentation with WIP indication"
      min_lines: 30
    - path: "LICENSE"
      provides: "Apache 2.0 license (already exists)"
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "Docker usage instructions reference compose file"
      pattern: "docker.compose"
---

<objective>
Create a concise README.md for the public GitHub repo with a clear WIP indication.

Purpose: Give visitors a quick understanding of what BeanBay is and how to run it, while signaling the project is actively under development.
Output: README.md — concise, factual, WIP-flagged.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rebrand-cleanup/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create concise README.md with WIP indication</name>
  <files>README.md</files>
  <action>
    Create a concise README.md. Keep it short and to the point — this is a WIP project, not a polished product page yet. Include:

    1. **Header:** BeanBay name with a WIP badge/note (e.g., "> **Note:** This project is under active development." or a bold WIP indicator near the top)

    2. **One-liner:** What it is — "Self-hosted coffee optimization powered by Bayesian learning"

    3. **Brief description** (3-5 sentences max):
       - Phone-first web app for espresso recipe optimization
       - Uses BayBE (Bayesian optimization) to learn from each brew and recommend better recipes
       - Self-hosted via Docker

    4. **Quick Start** — Docker:
       ```bash
       docker run -d --name beanbay -p 8000:8000 -v beanbay-data:/data ghcr.io/grzonka/beanbay:latest
       ```

    5. **Development Setup** (brief):
       ```bash
       git clone https://github.com/grzonka/beanbay.git
       cd beanbay
       uv sync
       alembic upgrade head
       uvicorn app.main:app --reload
       ```

    6. **Tech Stack** — one line: FastAPI, Jinja2/htmx, SQLite, BayBE, Docker

    7. **License** — Apache 2.0 (reference LICENSE file)

    Do NOT add:
    - Screenshot placeholders or badges that reference URLs that don't exist yet
    - Detailed feature lists (save for later)
    - Extensive "How It Works" sections
    - Roadmap details

    Keep the total README under ~60 lines. Concise and honest.
  </action>
  <verify>README.md exists, contains WIP indication, has Docker quick start, references Apache 2.0 license</verify>
  <done>README.md is concise, clearly marked as WIP, and has enough info for someone to understand and run BeanBay</done>
</task>

</tasks>

<verification>
- README.md renders well on GitHub (check markdown formatting)
- README.md contains WIP indication
- README.md references Apache 2.0 license (LICENSE file already present)
- No mention of MIT license anywhere
</verification>

<success_criteria>
- README.md exists with WIP indication and Docker quick start
- LICENSE file already present (Apache 2.0, unchanged)
- README references correct license
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-release/08-01-SUMMARY.md`
</output>
