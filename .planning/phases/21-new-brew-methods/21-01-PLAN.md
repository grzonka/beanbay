---
phase: 21-new-brew-methods
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/brew/_recipe_card.html
  - app/templates/history/_shot_modal.html
  - app/routers/history.py
  - app/routers/brew.py
autonomous: true

must_haves:
  truths:
    - "Recipe card shows steep_time for french-press, aeropress, cold-brew methods"
    - "Recipe card shows brew_volume for non-espresso methods"
    - "Recipe card hides temperature for cold-brew (no heated water)"
    - "Recipe card shows bloom_weight for pour-over"
    - "Shot detail modal shows method-specific params (not hardcoded espresso-only)"
    - "Brew index page shows method label on active setup"
  artifacts:
    - path: "app/templates/brew/_recipe_card.html"
      provides: "Method-aware param rendering"
    - path: "app/templates/history/_shot_modal.html"
      provides: "Method-aware shot detail"
    - path: "app/routers/history.py"
      provides: "Phase 21 columns in _load_shot_detail"
    - path: "app/routers/brew.py"
      provides: "Method label in brew_index context"
  key_links:
    - from: "app/routers/history.py"
      to: "app/templates/history/_shot_modal.html"
      via: "_load_shot_detail includes steep_time, brew_volume, bloom_weight, brew_mode"
    - from: "app/templates/brew/_recipe_card.html"
      to: "rec object"
      via: "conditional rendering based on param presence"
---

<objective>
Make all brew templates method-aware — recipe card, shot modal, and brew index correctly display parameters for all 7 brew methods (not just espresso).

Purpose: Users brewing with new methods (french-press, aeropress, cold-brew, turkish, moka-pot) see the correct parameters everywhere — recipe cards, shot history, and brew page. Espresso remains unchanged.

Output: Updated templates and router code that render correctly for any method.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/templates/brew/_recipe_card.html
@app/templates/history/_shot_modal.html
@app/routers/history.py
@app/routers/brew.py
@app/services/parameter_registry.py
@app/models/measurement.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Method-aware recipe card and brew index</name>
  <files>
    app/templates/brew/_recipe_card.html
    app/templates/brew/index.html
    app/routers/brew.py
  </files>
  <action>
Update `_recipe_card.html` to display method-specific params:

1. **Temperature conditional**: Wrap the temperature div in `{% if rec is mapping %}{% if rec.temperature is defined and rec.temperature is not none %}...{% endif %}{% else %}{% if rec.temperature is not none %}...{% endif %}{% endif %}`. Cold-brew has no temperature — it should not show "Temp: None°C".

2. **Steep time**: Add a new recipe-param block for `steep_time` after the dose row. Follow the same dual pattern (mapping vs ORM) used for other params. Label: "Steep", unit: "s" for seconds. Show only when present (not none).

3. **Bloom weight**: Add a recipe-param block for `bloom_weight` after dose. Label: "Bloom", unit: "g". Show only when present.

4. **Brew volume already partially handled** — the card shows it as fallback when target_yield is absent. Verify this works for all non-espresso methods. For methods that have BOTH target_yield and brew_volume (this doesn't currently happen but is future-safe), show brew_volume in its own row.

5. **Method label on brew index**: In `brew.py` `brew_index()`, add `method` to the template context (derive from active setup via `_get_method_from_setup(active_setup)`). In `index.html`, show the method name as a badge next to the setup name:
   ```html
   {% if active_setup %}
     <span class="badge badge-outline badge-sm capitalize">{{ method }}</span>
   {% endif %}
   ```
   Place it after the setup name div, before the equipment parts list.

  </action>
  <verify>
Run `.venv/bin/pytest tests/test_brew.py tests/test_brew_phase20.py tests/test_brew_phase21.py -q` — all pass.
Visually inspect: the recipe card template has conditional temperature, steep_time block, bloom_weight block.
  </verify>
  <done>
Recipe card renders steep_time, bloom_weight, brew_volume for non-espresso methods. Temperature hidden for cold-brew. Brew index shows method label.
  </done>
</task>

<task type="auto">
  <name>Task 2: Method-aware shot detail modal and history wiring</name>
  <files>
    app/routers/history.py
    app/templates/history/_shot_modal.html
  </files>
  <action>
1. **`_load_shot_detail` in history.py**: Add Phase 21 columns to the returned dict:
   - `"steep_time": m.steep_time`
   - `"brew_volume": m.brew_volume`
   - `"bloom_weight": m.bloom_weight`
   - `"brew_mode": m.brew_mode`
   
   Also add Phase 20 columns that are currently missing:
   - `"preinfusion_time": m.preinfusion_time`
   - `"preinfusion_pressure": m.preinfusion_pressure`
   - `"brew_pressure": m.brew_pressure`
   - `"pressure_profile": m.pressure_profile`
   - `"bloom_pause": m.bloom_pause`
   - `"flow_rate": m.flow_rate`
   - `"temp_profile": m.temp_profile`
   
   This ensures the shot modal shows ALL params, not just the original espresso-only set.

2. **`_shot_modal.html`**: Replace the hardcoded 6-param recipe grid with a dynamic approach:
   - Keep grind_setting always shown (it's universal).
   - Make temperature conditional: `{% if shot.temperature is not none %}` — hide for cold-brew.
   - Replace hardcoded pre-inf/yield/saturation with conditional blocks for ALL known params.
   
   Use this pattern for each optional param (after grind and temperature):
   ```html
   {% if shot.preinfusion_time is not none %}
   <div class="recipe-param">
     <span class="recipe-label">Pre-inf Time</span>
     <span class="recipe-value text-lg">{{ shot.preinfusion_time }}<span class="recipe-unit">s</span></span>
   </div>
   {% elif shot.preinfusion_pressure_pct is not none %}
   <div class="recipe-param">
     <span class="recipe-label">Pre-inf</span>
     <span class="recipe-value text-lg">{{ shot.preinfusion_pressure_pct }}<span class="recipe-unit">%</span></span>
   </div>
   {% endif %}
   ```
   
   Add blocks for: dose_in (always), target_yield (if not none), brew_volume (if not none), steep_time (if not none), bloom_weight (if not none), brew_pressure (if not none), pressure_profile (if not none, badge style), flow_rate (if not none), brew_mode (if not none, badge style), saturation (if not none, badge style), bloom_pause (if not none), temp_profile (if not none, badge style).

   Keep the ratio, extraction time, notes, flavor profile, and flavor tags sections unchanged.

3. **`_build_shot_dicts` in history.py**: Add `"brew_method"` to the shot dict so the shot row can optionally display method. Derive from `m.brew_setup.brew_method.name` if brew_setup exists, else "espresso".

  </action>
  <verify>
Run `.venv/bin/pytest tests/test_history.py tests/test_brew_phase21.py -q` — all pass.
Verify `_load_shot_detail` includes all new keys. Verify `_shot_modal.html` has conditional blocks for steep_time, brew_volume, bloom_weight.
  </verify>
  <done>
Shot detail modal dynamically shows params for any brew method. `_load_shot_detail` returns all Phase 20 + Phase 21 columns. Shot row includes brew_method for display.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest -q` — all tests pass (408+ tests)
- Recipe card template has conditional blocks for: temperature, steep_time, bloom_weight, brew_volume
- Shot modal template renders correctly for espresso (backward compat) and new methods
- `_load_shot_detail` returns steep_time, brew_volume, bloom_weight, brew_mode, and all Phase 20 params
- Brew index template shows method badge next to active setup
</verification>

<success_criteria>
1. Recipe card shows steep_time, bloom_weight for methods that have them
2. Recipe card hides temperature for cold-brew
3. Shot detail modal shows all method-specific params (not hardcoded espresso-only)
4. `_load_shot_detail` includes ALL measurement params
5. Brew index shows method label
6. All existing tests pass — no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/21-new-brew-methods/21-01-SUMMARY.md`
</output>
