---
phase: 23-v030-pre-release-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/history.py
  - app/templates/history/_filter_panel.html
  - app/templates/brew/_recipe_card.html
  - app/static/css/input.css
autonomous: true

must_haves:
  truths:
    - "User can filter brew history by brew setup"
    - "Setup filter works with existing bean and min-taste filters"
    - "Recipe card parameters show a visible info icon that reveals a detailed description on tap/hover"
  artifacts:
    - path: "app/routers/history.py"
      provides: "Setup filter query parameter and setup list for template"
    - path: "app/templates/history/_filter_panel.html"
      provides: "Setup dropdown with htmx integration"
    - path: "app/templates/brew/_recipe_card.html"
      provides: "Styled info icon on each parameter card"
  key_links:
    - from: "app/templates/history/_filter_panel.html"
      to: "/history/shots"
      via: "hx-get with setup_id parameter"
      pattern: "hx-include.*setup_id"
    - from: "app/routers/history.py"
      to: "Measurement.brew_setup_id"
      via: "SQLAlchemy filter"
      pattern: "filter.*brew_setup_id"
---

<objective>
Add brew setup filtering to the history page and enhance recipe card info icons to be more visible and user-friendly.

Purpose: Users need to filter their brew history by equipment setup to compare results across setups. The existing parameter tooltips on recipe cards are too subtle — users need clearly visible info icons that explain what each parameter means.
Output: History page with 3-filter panel (bean + setup + min taste). Recipe cards with prominent light-brown info icons.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/routers/history.py
@app/templates/history/_filter_panel.html
@app/templates/history/index.html
@app/templates/brew/_recipe_card.html
@app/static/css/input.css
@app/models/brew_setup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brew setup filter to history page</name>
  <files>app/routers/history.py, app/templates/history/_filter_panel.html</files>
  <action>
  **Backend changes in `app/routers/history.py`:**

  1. Import BrewSetup model at the top: `from app.models.brew_setup import BrewSetup`

  2. Add `setup_id: Optional[str] = None` parameter to `_build_shot_dicts()` function signature. Add filter logic:
     ```python
     if setup_id:
         query = query.filter(Measurement.brew_setup_id == setup_id)
     ```

  3. Update `history_page()` route handler:
     - Add `setup_id: Optional[str] = None` query parameter
     - Query active (non-retired) BrewSetups: `setups = db.query(BrewSetup).filter(BrewSetup.is_retired.is_(False)).order_by(BrewSetup.name).all()`
     - Pass `setup_id` to `_build_shot_dicts()`
     - Add `setups=setups` and `filter_setup_id=setup_id` to template context

  4. Update `history_shots_partial()` route handler:
     - Add `setup_id: Optional[str] = None` query parameter
     - Pass `setup_id` to `_build_shot_dicts()`

  **Template changes in `app/templates/history/_filter_panel.html`:**

  Add a third `<select>` dropdown for brew setup between the bean and min-taste dropdowns. Follow the exact same htmx pattern as the existing dropdowns:

  ```html
  <div class="form-control flex-1">
    <label class="label" for="filter-setup"><span class="label-text">Setup</span></label>
    <select id="filter-setup" name="setup_id" class="select select-bordered w-full min-h-12"
      hx-get="/history/shots" hx-target="#shot-list" hx-swap="innerHTML"
      hx-include="[name='bean_id'],[name='min_taste']">
      <option value="">All setups</option>
      {% for setup in setups %}
      <option value="{{ setup.id }}" {{ 'selected' if filter_setup_id == setup.id else '' }}>
        {{ setup.name }}
      </option>
      {% endfor %}
    </select>
  </div>
  ```

  **Critical:** Update the existing bean and min-taste dropdowns' `hx-include` attributes to also include `[name='setup_id']`. Each dropdown must include ALL other filter names:
  - Bean dropdown: `hx-include="[name='min_taste'],[name='setup_id']"`
  - Setup dropdown: `hx-include="[name='bean_id'],[name='min_taste']"`
  - Min taste dropdown: `hx-include="[name='bean_id'],[name='setup_id']"`

  Also update `app/templates/history/index.html` to pass `setups` to the filter panel include if it uses a separate include mechanism (check if `_filter_panel.html` receives vars from parent context or needs explicit passing).
  </action>
  <verify>
  - Run `python -m pytest tests/test_history.py -x` to ensure existing history tests pass
  - Run the dev server and navigate to `/history` — verify three dropdowns appear
  - Select a setup → verify the shot list filters correctly
  - Combine setup filter with bean filter → verify both apply
  - Clear setup filter ("All setups") → verify full list returns
  </verify>
  <done>History page shows bean, setup, and min-taste filter dropdowns. All three filters work together via htmx. Selecting a setup filters shots to only those recorded with that setup.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance recipe card info icons for better visibility</name>
  <files>app/templates/brew/_recipe_card.html, app/static/css/input.css</files>
  <action>
  The existing `_recipe_card.html` already has info icon tooltips (lines 22-28) but they use a tiny, low-opacity SVG that's nearly invisible. The user wants clearly visible light-brown "i" icons in a consistent position.

  **Update `_recipe_card.html` `param_block` macro:**

  Replace the current tooltip SVG implementation (lines 22-28) with a more visible info icon. Use a small circular badge with "i" text instead of the nearly-invisible SVG:

  ```html
  {% if desc %}
    <span class="tooltip tooltip-top ml-1 cursor-help align-middle inline-flex items-center justify-center w-4 h-4 rounded-full text-xs font-bold param-info-icon" data-tip="{{ desc }}">i</span>
  {% endif %}
  ```

  **Add CSS in `app/static/css/input.css`** (in the `@layer components` section, near the recipe-param styles):

  ```css
  .param-info-icon {
    background: oklch(55% 0.08 60);
    color: oklch(90% 0.02 70);
    font-size: 0.6rem;
    line-height: 1;
    flex-shrink: 0;
  }
  ```

  This creates a small light-brown circle with a cream-colored "i" that matches the espresso theme. The `oklch(55% 0.08 60)` is a warm brown that's visible but not distracting.

  **Important:** Keep the existing `data-tip` daisyUI tooltip mechanism — it works well. We're only changing the visual trigger from an invisible SVG to a visible brown "i" badge.

  After editing `input.css`, rebuild the CSS: `npx @tailwindcss/cli -i app/static/css/input.css -o app/static/css/main.css --minify` (or check the project's Makefile for the CSS build command).
  </action>
  <verify>
  - Check Makefile or package.json for CSS build command and run it
  - Run `python -m pytest tests/ -x --timeout=30` to ensure nothing breaks
  - Run the dev server, navigate to `/brew`, get a recommendation, and verify each parameter card shows a small light-brown "i" icon
  - Hover/tap the "i" icon — verify the tooltip appears with the parameter description
  - Verify the icon is visible but not distracting — small circle, warm brown color
  </verify>
  <done>Each recipe parameter card on the recommendation page displays a small light-brown circular "i" info icon that reveals the parameter description on hover/tap via daisyUI tooltip.</done>
</task>

</tasks>

<verification>
- All existing tests pass (`python -m pytest tests/ -x`)
- History page loads with three filter dropdowns
- All three filters (bean, setup, min taste) work independently and in combination
- Recipe cards show visible info icons with working tooltips
- CSS builds without errors
</verification>

<success_criteria>
- History page has a "Setup" filter dropdown populated with active brew setups
- Selecting a setup filters the shot list to only shots from that setup
- All three filters combine correctly (bean + setup + min taste)
- Recipe parameter cards display a light-brown "i" icon badge
- Tapping/hovering the icon shows the parameter description tooltip
</success_criteria>

<output>
After completion, create `.planning/phases/23-v030-pre-release-fixes/23-01-SUMMARY.md`
</output>
