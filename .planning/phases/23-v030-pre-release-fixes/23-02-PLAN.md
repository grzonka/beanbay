---
phase: 23-v030-pre-release-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/brew/recommend.html
  - app/templates/brew/manual.html
  - app/templates/brew/best.html
  - app/main.py
  - app/templates/welcome.html
autonomous: true

must_haves:
  truths:
    - "Non-espresso brews show 'Rate This Brew' instead of 'Rate This Shot'"
    - "Non-espresso brews allow extraction times longer than 2 minutes"
    - "Espresso brews still show 'Rate This Shot' and 'Failed shot' language"
    - "New users see a welcome page explaining what BeanBay does"
    - "Welcome page guides users to add beans and start brewing"
  artifacts:
    - path: "app/templates/brew/recommend.html"
      provides: "Method-aware rating section with dynamic heading and time range"
    - path: "app/templates/welcome.html"
      provides: "Welcome landing page"
    - path: "app/main.py"
      provides: "Root route serving welcome page for new users"
  key_links:
    - from: "app/main.py"
      to: "/welcome or /beans"
      via: "conditional redirect based on whether beans exist"
      pattern: "db.query.*Bean.*count"
    - from: "app/templates/brew/recommend.html"
      to: "method variable"
      via: "Jinja2 conditional"
      pattern: "method.*espresso"
---

<objective>
Make brew evaluation method-aware (terminology + time ranges) and add a welcome page for new users.

Purpose: Non-espresso brews (french-press, pour-over, cold-brew, etc.) shouldn't say "Rate This Shot" or "Failed shot" — those are espresso terms. Similarly, limiting extraction time to 120 seconds is wrong for methods that can take 4+ minutes (or hours for cold brew). A welcome page helps new users understand what BeanBay does before diving in.
Output: Method-aware evaluation forms. Welcome page at `/` for first-time visitors.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/templates/brew/recommend.html
@app/templates/brew/manual.html
@app/templates/brew/best.html
@app/templates/base.html
@app/main.py
@app/routers/brew.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make brew evaluation method-aware</name>
  <files>app/templates/brew/recommend.html, app/templates/brew/manual.html, app/templates/brew/best.html</files>
  <action>
  All three evaluation templates need method-aware adjustments. The `method` variable is already available in all three templates (passed from the route handler or available via `rec.method`).

  **In `recommend.html`:**

  1. Change the heading (line 68) from hardcoded "Rate This Shot" to method-aware:
     ```html
     <h2 class="card-title mb-4">
       {% if method == 'espresso' or not method %}Rate This Shot{% else %}Rate This Brew{% endif %}
     </h2>
     ```
     Note: `method` may not be directly available in recommend.html — check the template context. It can be derived from `rec.method` if `rec` is a dict: `{% set brew_method = rec.method if rec is mapping and rec.method is defined else 'espresso' %}`

  2. Change the extraction time input (lines 122-137). Make the label, placeholder, min, and max method-aware:
     ```html
     {% if brew_method == 'espresso' %}
       <label class="label" for="extraction_time">
         <span class="label-text">Extraction Time (seconds, optional)</span>
       </label>
       <input type="number" id="extraction_time" name="extraction_time"
         class="input input-bordered w-full" placeholder="e.g. 28"
         min="5" max="120" step="1">
     {% elif brew_method == 'cold-brew' %}
       <label class="label" for="extraction_time">
         <span class="label-text">Brew Time (minutes, optional)</span>
       </label>
       <input type="number" id="extraction_time" name="extraction_time"
         class="input input-bordered w-full" placeholder="e.g. 720"
         min="60" max="2880" step="1">
     {% else %}
       <label class="label" for="extraction_time">
         <span class="label-text">Brew Time (seconds, optional)</span>
       </label>
       <input type="number" id="extraction_time" name="extraction_time"
         class="input input-bordered w-full" placeholder="e.g. 240"
         min="5" max="1800" step="1">
     {% endif %}
     ```
     For non-espresso: max=1800 (30 minutes) covers french-press (4-8 min), pour-over (3-5 min), aeropress (1-4 min), turkish (3-5 min), moka-pot (3-6 min). For cold-brew: max=2880 (48 hours in minutes).

  3. Change "Failed shot" text to method-aware:
     ```html
     <span class="label-text">
       {% if brew_method == 'espresso' or not brew_method %}
         ⚠️ Failed shot (choked / gusher)
       {% else %}
         ⚠️ Failed brew
       {% endif %}
     </span>
     ```
     And the help text below:
     ```html
     <span class="label-text-alt">Auto-sets taste to 1 — still teaches the optimizer what not to do.</span>
     ```
     (This can stay the same for both — it's generic enough.)

  **In `manual.html`:**
  Apply the same changes. The `method` variable is available directly. Apply same extraction time range changes and "Failed shot" → "Failed brew" for non-espresso.

  **In `best.html`:**
  Apply the same changes. The `method` variable is available via the template context. Check the route handler (`show_best_recipe` in brew.py) — it may need to pass `method` explicitly if not already doing so. The `rec` object should carry the method.

  **Approach for determining method in templates:**
  At the top of each template's evaluation section, set a local variable:
  ```html
  {% set brew_method = method if method is defined else (rec.method if rec is mapping and rec.method is defined else 'espresso') %}
  ```
  This ensures backward compatibility — if method isn't explicitly passed, it defaults to espresso.
  </action>
  <verify>
  - Run `python -m pytest tests/ -x --timeout=30`
  - Run the dev server, select a french-press setup, get a recommendation → verify heading says "Rate This Brew"
  - Verify extraction time allows values > 120 seconds for non-espresso
  - Verify failed toggle says "Failed brew" for non-espresso
  - Switch to espresso setup → verify heading says "Rate This Shot", time max=120, "Failed shot"
  </verify>
  <done>Non-espresso evaluation forms show "Rate This Brew", allow brew times up to 1800s (or 2880min for cold-brew), and show "Failed brew" instead of "Failed shot". Espresso forms remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create welcome page for new users</name>
  <files>app/main.py, app/templates/welcome.html</files>
  <action>
  **Create `app/templates/welcome.html`:**

  A clean, friendly welcome page using daisyUI components and the espresso theme. The page should:

  1. Extend `base.html`
  2. Show a welcome hero section with:
     - App name "BeanBay" as heading
     - Tagline: "Your personal coffee optimization companion"
     - Brief explanation (2-3 sentences): BeanBay helps you dial in your perfect coffee. Log your brews, rate the taste, and let Bayesian optimization suggest better recipes — learning from every cup you make.
  3. Show 3-4 feature cards in a grid explaining what users can do:
     - **Track Brews** — Log every brew with recipe parameters and taste ratings
     - **Get Smarter Recipes** — Bayesian optimization learns from your feedback to suggest better parameters
     - **Multiple Methods** — Espresso, pour-over, french press, AeroPress, and more
     - **Equipment Profiles** — Set up your grinder, brewer, and water recipe for personalized recommendations
  4. A prominent CTA button: "Add Your First Bean →" linking to `/beans`
  5. A secondary link: "I already have beans" linking to `/brew` (for returning users who somehow land here)

  Use daisyUI classes: `hero`, `card`, `btn btn-primary`, grid layout. Keep it warm and inviting, matching the espresso theme. No images needed — use emoji or simple text icons (☕, 🎯, 📊, ⚙️).

  **Update `app/main.py` root route:**

  Change the `root()` handler (currently a simple redirect to `/beans`) to conditionally show the welcome page for new users:

  ```python
  @app.get("/", response_class=HTMLResponse)
  async def root(request: Request, db: Session = Depends(get_db)):
      """Show welcome page if no beans exist, otherwise redirect to beans."""
      from app.models.bean import Bean
      bean_count = db.query(Bean).count()
      if bean_count == 0:
          return templates.TemplateResponse(request, "welcome.html")
      return RedirectResponse(url="/beans", status_code=303)
  ```

  This way:
  - **New users** (no beans) → see the welcome page
  - **Returning users** (have beans) → go straight to beans list as before

  **Note:** The root route currently imports from FastAPI and uses RedirectResponse. You'll need to add HTMLResponse, Request, Depends, and the template setup. Check how other routes in the app handle this — likely import `templates` from a shared location. Look at `app/routers/brew.py` or `app/routers/beans.py` for the pattern. The `main.py` may need to set up its own Jinja2Templates instance or reuse the one from a router.

  Also add "Home" or "Welcome" to the sidebar in `base.html` — actually, no. The BeanBay logo already links to `/beans` (which effectively becomes `/` for returning users). No sidebar change needed.
  </action>
  <verify>
  - Run `python -m pytest tests/ -x --timeout=30`
  - Start with a fresh/empty database → navigate to `/` → verify welcome page appears
  - Add a bean → navigate to `/` → verify redirect to `/beans`
  - Verify welcome page looks good on mobile and desktop
  - Click "Add Your First Bean" → verify navigation to `/beans`
  </verify>
  <done>New users (no beans) see a welcome page at `/` explaining BeanBay's features with a CTA to add their first bean. Returning users (have beans) are redirected to `/beans` as before.</done>
</task>

</tasks>

<verification>
- All existing tests pass (`python -m pytest tests/ -x`)
- Non-espresso brews show method-appropriate language throughout evaluation
- Extraction time input allows appropriate ranges per brew method
- Welcome page renders for users with no beans
- Welcome page redirects to beans for users with existing beans
</verification>

<success_criteria>
- "Rate This Shot" appears only for espresso; other methods show "Rate This Brew"
- "Failed shot" appears only for espresso; other methods show "Failed brew"
- Extraction time max is 120s for espresso, 1800s for most methods, 2880min for cold-brew
- Welcome page at `/` shows for empty databases
- Welcome page has clear feature explanation and CTA to add first bean
</success_criteria>

<output>
After completion, create `.planning/phases/23-v030-pre-release-fixes/23-02-SUMMARY.md`
</output>
